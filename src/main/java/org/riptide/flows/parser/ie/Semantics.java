package org.riptide.flows.parser.ie;

public enum Semantics {
    DEFAULT,
    QUANTITY,
    TOTAL_COUNTER,
    DELTA_COUNTER,
    IDENTIFIER,
    FLAGS,
    LIST,
    SNMP_COUNTER,
    SNMP_GAUGE,
}
