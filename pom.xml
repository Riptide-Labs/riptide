<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>org.riptide.flows</groupId>
    <artifactId>riptide-flows</artifactId>
    <version>0.12.0</version>
    <name>riptide-flows</name>
    <description>NetFlow analysis engine</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <build-helper-maven-plugin.version>3.6.1</build-helper-maven-plugin.version>
        <caffeine.version>3.2.4</caffeine.version>
        <common-csv.version>1.14.1</common-csv.version>
        <dnsjava.version>3.6.5</dnsjava.version>
        <guava.version>33.7.1-jre</guava.version>
        <ipaddress.version>5.6.2</ipaddress.version>
        <!-- Overrides Spring Boot's managed jackson-2-bom.version (2.21.4), which governs every
             Jackson 2 module. 2.22.1 patches GHSA-5jmj-h7xm-6q6v (Scorecard/osv-scanner finding).
             Set on the BOM rather than pinning jackson-databind alone so core and annotations move
             with it: Jackson expects databind and core on the same minor, and databind is a direct
             dependency on a reachable path (the MCP components parse JSON-RPC frames and ClickHouse
             JSONEachRow rows with it). Configuration binding no longer goes through Jackson, see
             #434. Drop once the managing parent catches up. -->
        <jackson-2-bom.version>2.22.1</jackson-2-bom.version>
        <jakarta-xml-bind-api.version>4.0.5</jakarta-xml-bind-api.version>
        <java.version>25</java.version>
        <jetbrains-annotations.version>26.1.0</jetbrains-annotations.version>
        <lombok.version>1.18.46</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <maxmind-db.version>4.1.0</maxmind-db.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <versions-maven-plugin.version>2.21.0</versions-maven-plugin.version>
        <!-- Overrides Spring Boot's managed netty.version (4.2.15.Final), which governs every
             io.netty:netty-* module and otherwise leaves them a patch behind netty-all. 4.2.16.Final
             patches the Netty advisories in Scorecard alert #120 (GHSA-hpcc-26xq-25fv and 15 more). -->
        <netty.version>4.2.17.Final</netty.version>
        <!-- Overrides Spring Boot's managed httpcore5.version (5.4.2). 5.4.3 patches the HTTP/1
             header-parsing DoS in Scorecard alert #120 (GHSA-hf6x-8p5f-cgmf). -->
        <httpcore5.version>5.4.3</httpcore5.version>
        <!-- Overrides Spring Boot's managed httpclient5.version (5.6.1), pulled in by the
             ClickHouse client-v2 transport. 5.6.3 patches the Content-Encoding decode
             connection leak in Scorecard alert #120 (GHSA-hjcp-jmpx-g3qm). -->
        <httpclient5.version>5.6.3</httpclient5.version>
        <!-- Overrides Spring Boot's managed log4j2.version (2.25.4), present only as the
             log4j-to-slf4j bridge. 2.25.5 patches the MapMessage JSON serialization issue
             in Scorecard alert #120 (GHSA-qv9r-c865-cp47). -->
        <log4j2.version>2.25.5</log4j2.version>
        <org-eclipse-persistence-moxy.version>5.0.1</org-eclipse-persistence-moxy.version>
        <resilience4j.version>2.4.0</resilience4j.version>
        <snmp4j.version>3.13.1</snmp4j.version>
        <error-prone.version>2.50.0</error-prone.version>
        <spotbugs-maven-plugin.version>4.10.4.0</spotbugs-maven-plugin.version>
        <snmp4j-agent.version>3.10.1</snmp4j-agent.version>
        <spring-vault.version>4.1.0</spring-vault.version>
        <dropwizard-metrics.version>4.2.39</dropwizard-metrics.version>
        <clickhouse.version>0.10.0</clickhouse.version>
        <jmh.version>1.37</jmh.version>
        <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
        <testcontainers.version>2.0.5</testcontainers.version>
        <jazzer.version>0.30.0</jazzer.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Jackson and Netty are patched by overriding the versions their BOMs are imported
                 with (jackson-2-bom.version, netty.version), not by pinning single artifacts here. -->
            <!-- Transitive-CVE pin (Scorecard/osv-scanner finding): lz4-java vulnerability (GHSA-xx22-p4ch-683r) -->
            <dependency>
                <groupId>at.yawk.lz4</groupId>
                <artifactId>lz4-java</artifactId>
                <version>1.11.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- helper for property/yaml auto-completion -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.dropwizard.metrics</groupId>
            <artifactId>metrics-core</artifactId>
            <version>${dropwizard-metrics.version}</version>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>${guava.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-circuitbreaker</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>dnsjava</groupId>
            <artifactId>dnsjava</artifactId>
            <version>${dnsjava.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>${caffeine.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>${common-csv.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- parsing of XML (JAXB API) -->
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>${jakarta-xml-bind-api.version}</version>
        </dependency>

        <!-- parsing of XML (JAXB Impl) -->
        <dependency>
            <groupId>org.eclipse.persistence</groupId>
            <artifactId>org.eclipse.persistence.moxy</artifactId>
            <version>${org-eclipse-persistence-moxy.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- JetBrains annotations (@NotNull, @Language) used in sources; compile-time only.
             Was previously available transitively via resilience4j 2.3.x -> kotlin-stdlib. -->
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>${jetbrains-annotations.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- support for lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- mapstruct -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>

        <!-- SNMP support -->
        <dependency>
            <groupId>org.snmp4j</groupId>
            <artifactId>snmp4j</artifactId>
            <version>${snmp4j.version}</version>
        </dependency>

        <dependency>
            <groupId>com.github.seancfoley</groupId>
            <artifactId>ipaddress</artifactId>
            <version>${ipaddress.version}</version>
        </dependency>

        <!-- GeoIP enrichment (MaxMind/IPinfo mmdb reader) -->
        <dependency>
            <groupId>com.maxmind.db</groupId>
            <artifactId>maxmind-db</artifactId>
            <version>${maxmind-db.version}</version>
        </dependency>

        <!-- Clickhouse -->
        <dependency>
            <groupId>com.clickhouse</groupId>
            <artifactId>client-v2</artifactId>
            <version>${clickhouse.version}</version>
        </dependency>

        <!-- HashiCorp Vault (SecretRef vault:// resolver). Deliberately spring-vault-core, not
             spring-cloud-starter-vault-config: SecretRefs resolve at poll time via VaultTemplate,
             not at boot time via property sources. -->
        <dependency>
            <groupId>org.springframework.vault</groupId>
            <artifactId>spring-vault-core</artifactId>
            <version>${spring-vault.version}</version>
        </dependency>

        <!-- Test scope -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.snmp4j</groupId>
            <artifactId>snmp4j-agent</artifactId>
            <version>${snmp4j-agent.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-vault</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Benchmarks -->
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>${jmh.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Coverage-guided fuzzing of the flow parsers (@FuzzTest). In a normal `mvn verify`
             (JAZZER_FUZZ unset) these run in regression mode: the seed corpus and any crash
             inputs replay as ordinary JUnit tests, no native agent. The nightly workflow sets
             JAZZER_FUZZ=1 to actually fuzz. See src/test/java/org/riptide/flows/fuzz. -->
        <dependency>
            <groupId>com.code-intelligence</groupId>
            <artifactId>jazzer-junit</artifactId>
            <version>${jazzer.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- Lombok is declared <scope>provided</scope>, but repackage
                         bundles provided-scope dependencies into BOOT-INF/lib
                         anyway. It is a compile-time annotation processor with
                         nothing left to do at runtime, and it carries a nested
                         lombok/launch/mavenEcjBootstrapAgent.jar that syft
                         catalogs as an unversioned, unlicensed package. -->
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <release>${java.version}</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.openjdk.jmh</groupId>
                            <artifactId>jmh-generator-annprocess</artifactId>
                            <version>${jmh.version}</version>
                        </path>
                        <path>
                            <groupId>com.google.errorprone</groupId>
                            <artifactId>error_prone_core</artifactId>
                            <version>${error-prone.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-XDcompilePolicy=simple</arg>
                        <arg>--should-stop=ifError=FLOW</arg>
                        <!-- The rule, not a count: a check reaches this list once its sites are at zero, and it stays
                             there because the build says so. Every check below is warning-free or explicitly suppressed
                             at the point it was added. A check left at WARN with known sites is the state this list
                             exists to prevent — it looks like coverage and provides none, because occurrence N+1 is
                             indistinguishable from the N that were always there. Worse on this forge specifically:
                             GitHub caps warning annotations at ten per job, so past ten the surface a maintainer reads
                             stops being a measurement. That is not hypothetical here. The backlog these checks were
                             added to clear stood at 16, while the annotation view showed 10 and had been showing 10 for
                             as long as anyone had looked.
                             UnrecognisedJavadocTag is off, which is a different claim: the check itself is wrong here.
                             It cannot see straight through Lombok — Lombok copies a field's Javadoc onto the accessors
                             it generates, and Error Prone then re-parses that copy and reports every inline
                             {@code}/{@link} in it as an unrecognised tag. The source is valid — `javac -Xdoclint:all`
                             on the same files reports no malformed tag — and deleting @Data from ClickhouseConfig takes
                             its seven warnings to zero, which is the whole of the evidence. Turning it off beats
                             degrading real Javadoc to satisfy a false positive. -->
                        <arg>-Xplugin:ErrorProne -XepExcludedPaths:.*/target/generated-.*sources/.* -Xep:EqualsGetClass:ERROR -Xep:NarrowingCompoundAssignment:ERROR -Xep:ExposedPrivateType:ERROR -Xep:PatternMatchingInstanceof:ERROR -Xep:ReferenceEquality:ERROR -Xep:ModifyCollectionInEnhancedForLoop:ERROR -Xep:UnusedMethod:ERROR -Xep:UnnecessaryParentheses:ERROR -Xep:LongDoubleConversion:ERROR -Xep:NotJavadoc:ERROR -Xep:CanonicalDuration:ERROR -Xep:UnsafeReflectiveConstructionCast:ERROR -Xep:UnrecognisedJavadocTag:OFF</arg>
                        <arg>
                            -Amapstruct.unmappedTargetPolicy=ERROR
                        </arg>
                        <arg>
                            -Amapstruct.unmappedSourcePolicy=ERROR
                        </arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <!-- The Jazzer fuzz harnesses live in src/fuzz/java, not src/test/java, and this is the
                 plugin that puts them back on the test classpath. Two reasons for the odd location.
                 The harnesses are not unit tests: they are non-deterministic, unbounded in runtime,
                 and only run during a normal build because jazzer-junit replays the committed corpus
                 as regression tests. And OpenSSF Scorecard's fuzzing check cannot see them under
                 src/test: its file walker drops every path prefixed src/test/ (isTestdataFile in
                 checks/fileparser/listing.go) before it greps *.java for the Jazzer import, so a
                 harness in the conventional Maven location is invisible to the check that exists to
                 find it, and the repo scores 0 on Fuzzing while fuzzing nightly. See #474.

                 add-test-source, not add-source: the harnesses are test-scoped and must not land in
                 the shipped jar. Surefire selects them by compiled class name, so `make fuzz` and the
                 nightly matrix in fuzz.yml need no knowledge of this.

                 The validate binding and the position of this plugin ahead of maven-checkstyle-plugin
                 are both load-bearing. Checkstyle runs at validate with includeTestSourceDirectory
                 and reads testCompileSourceRoots; add-test-source defaults to generate-sources, which
                 is later. At the default phase Checkstyle would stop linting these five files without
                 failing, just silently covering less. Maven orders same-phase executions by
                 declaration order, so this must stay above Checkstyle. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>${build-helper-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>add-fuzz-source</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>add-test-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/fuzz/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- some basic checkstyle rules -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <configuration>
                    <configLocation>config/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <linkXRef>false</linkXRef>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs-maven-plugin.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <excludeFilterFile>config/spotbugs-exclude.xml</excludeFilterFile>
                </configuration>
                <executions>
                    <execution>
                        <id>check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>${versions-maven-plugin.version}</version>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <!-- floors just below the unit-only baseline
                                             (66.6% instruction / 57.3% branch, 2026-07-12)
                                             so coverage cannot silently regress -->
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.65</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.55</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Integration and e2e tests (*IT classes) need Docker; kept out of the default build. -->
        <profile>
            <id>e2e</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <repositories>
        <repository>
            <id>maven_central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2/</url>
        </repository>
    </repositories>
</project>
